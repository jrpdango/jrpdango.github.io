<script setup lang="ts">
defineEmits(['themeChanged']);
</script>

<template>
    <label class="theme-switch" for="switch">
        <input type="checkbox" id="switch" @click="$emit('themeChanged')" />
        <div class="slider"></div>
    </label>
</template>

<style scoped>
   .theme-switch {
        display: inline-block;
        height: 34px;
        position: relative;
        width: 60px;
    } 

    .theme-switch input {
        display: none;
    }

    .slider {
        background-color: #ccc;
        bottom: 0;
        cursor: pointer;
        left: 0;
        position: absolute;
        right: 0;
        top: 0;
        transition: .4s;
        border-radius: 34px;
    }

    .slider:before {
        background-color: #fff;
        bottom: 4px;
        content: "";
        height: 26px;
        left: 4px;
        position: absolute;
        transition: .4s;
        width: 26px;
        border-radius: 50%;
    }

    input:checked + .slider {
        background-color: #66bb6a;
    }

    input:checked + .slider:before {
        transform: translateX(26px);
    }
</style>